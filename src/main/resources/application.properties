spring.application.name=biovault

# Security (using defaults if env vars not set)
spring.security.user.name=${SECURITY_USERNAME:admin}
spring.security.user.password=${SECURITY_PASSWORD:admin123}

# MongoDB connection – hardcode Atlas URI (to bypass .env issues)
spring.data.mongodb.uri=mongodb+srv://biovault-admin:rFy7aGEaGVi7F1CB@biovault.dvebb58.mongodb.net/?appName=BioVault
spring.data.mongodb.database=biovault-db

# Server port
server.port=8080

# Enable debug logging to see endpoint mappings (optional, helps verify your controller is registered)
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=DEBUG

# If you have JPA dependencies, uncomment the line below:
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration